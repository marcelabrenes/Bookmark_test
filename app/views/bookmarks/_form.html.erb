<%= form_with(model: bookmark,data: {confirm: 'Save Bookmark?'}, local: false) do |form| %>
  <% if bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark.errors.count, "error") %> prohibited this bookmark from being saved:</h2>

      <ul>
        <% bookmark.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <h5><%= form.label "Select categories" %></h5> 
    <%= form.select :category_id, Category.pluck(:name, :id) %>

  </div>
  
  <div class="field">
    <h5><%= form.label "Select kinds" %></h5>  
    <%= form.select :kind_id, Kind.pluck(:name, :id) %>
  
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
